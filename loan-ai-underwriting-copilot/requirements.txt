# Runtime dependencies for the analysis nodes
requests>=2.31.0,<3
jsonschema>=4.21.0,<5

# Deployment / demo tooling (not needed inside the Dataloop runtime, which
# already provides dtlpy)
dtlpy>=1.100.0,<2

# Tests
pytest>=8.0.0,<9
